import React, { useEffect, useState } from "react";

const Contact= () => {
    return (
        <div id="Contact" className="flex flex-col items-center justify-center h-screen bg-gradient-to-b from-dark-blue to-darker-blue">
            <h1 className="font-mono font-semibold text-4xl">Contact</h1>
        </div>
    );
}

export default Contact;